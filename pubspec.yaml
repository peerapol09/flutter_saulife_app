name: flutter_saulife_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"


dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2 #ใช้ font แปลกๆ จาก google font ได้หมดเลย
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^11.0.0 #ใช้ icon แปลกๆ จาก font awesome ได้หมดเลย

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
